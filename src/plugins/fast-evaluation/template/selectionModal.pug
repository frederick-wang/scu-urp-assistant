#selection-modal
  form#selection-form.form-horizontal(role='form')
    .row
      .col-xs-12
        .selection-modal-introduction
          p 所有选中的老师都将被一键满分好评，主观评价会从25条语句库里随机抽取。
          p 默认所有老师都是选中状态，您只需要取消勾选您想手动评价的老师即可。
        hr
        each val in checkboxWrappers
          h4.lighter.blue= val[0]
          .checkbox-wrapper(id=val[1])
